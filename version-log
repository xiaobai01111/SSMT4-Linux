2026-02-20 0.0.4-beta

新增
- 新增下载后主程序自动识别能力（按预设已知路径优先，失败时回退扫描候选 `.exe`），并自动回写 `gamePath`。
- 新增后端命令 `resolve_downloaded_game_executable`，用于下载完成后的可执行文件解析。

优化
- 优化 HoYoverse 包选择逻辑：按 `bizPrefix` 精确匹配并结合接口地域提示过滤，减少跨游戏/跨区服串版本概率。
- 优化下载器并行下载链路与进度事件兼容，提升大体积资源下载稳定性与吞吐表现。
- 优化下载弹窗状态同步逻辑（服务器、版本状态、配置加载），减少切换游戏时的 UI 错位显示。

修改
- 移除 `HonkaiImpact3rd`（崩坏3）支持：包括预设、资源目录、图标配置、多语言文案及相关入口。
- 数据库启动同步新增兼容清理：自动删除旧库中 `HonkaiImpact3rd` 的 preset / identity / alias / config 残留。
