2026-03-13 0.0.9

新增与重构
- 继续深化后端模块拆分，补齐 `telemetry`、`settings`、`game_launcher`、`game_catalog_seed`、`bridge runtime`、`Hoyoverse 下载计划` 等高复杂度热点的职责边界。
- 新增多组前端与后端单元测试，覆盖启动守卫、下载任务状态、游戏更新检查上下文、运行时事件状态、游戏目录 seed 导入策略、telemetry hosts 管理等关键纯逻辑。

配置、数据库与目录状态
- 运行时配置读取进一步收敛到 SQLite `AppConfig` 与统一 runtime config service，减少 `bootstrap`、旧设置文件与运行时全局状态并存导致的漂移。
- `bootstrap` 配置链路继续收口为启动期加载与兼容迁移；前端不再承担“先调用 `load_settings` 才进入正确状态”的隐式职责。
- `game_catalog_seed` 改为仅在首建或 schema 升级时导入，不再在每次数据库初始化时参与日常补缺，降低 seed catalog、数据库与运行时 preset 的多源耦合。
- 数据库初始化失败不再被进程永久缓存，临时 I/O、权限或损坏修复后可在同一进程内重试恢复。
- 设置读写接口按读/写语义进一步拆分，减少命令层对数据库松散 API 的直接依赖。

启动链路、Bridge 与防护
- `game_launcher` 继续拆分为目标解析、运行时环境、命令构建、启动监控、日志策略等子模块，主入口进一步退化为编排层。
- 启动前环境日志与命令摘要增加敏感信息脱敏，避免代理、令牌、私有路径和自定义密钥直接写入游戏日志。
- 进程监控改为基于 `/proc`、根 PID、启动时间、启动路径与子进程树联合判定，降低 `pgrep -f` 模糊匹配带来的误判。
- `telemetry` / 防护子系统接口层改为强类型 payload，不再大量以 `serde_json::Value` 和手拼 JSON 作为命令返回值。
- `/etc/hosts` 管理增加可替换访问层，优先直接写入，失败时才回退 `pkexec tee`；同时保留受控 block 级恢复，避免误删用户自定义 hosts 规则。
- 防护应用链路补齐 staged rollback，避免出现“hosts 已改 / DLL 已删 / 渠道未改完”的半成功状态。
- Bridge stdout 协议改为更严格的消息解析，字段缺失、未知类型或级别漂移会在解析层直接暴露，不再静默退化成空值事件。
- Migoto / bridge 稳定字段继续向共享合同和共享默认值靠拢，降低前端、Rust 与 Windows bridge 三端重复维护成本。

下载、资源与更新
- 鸣潮更新链路优先走增量补丁；补丁不可用或失败时再回退全量比对更新，减少不必要的完整资源下载。
- GitHub release / XXMI / Migoto 包下载改为复用流式落盘与统一校验逻辑，不再整包读入内存。
- `Data-parameters` 资源策略收敛为发布态固定加载顺序；外部 Git 同步改为显式任务，不再在启动链路隐式 clone/pull。
- 游戏更新状态在前端启动与下载完成后会主动刷新，首页与下载页不再依赖重启应用才能看到最新更新状态。

前端页面与类型治理
- `Home`、`Settings`、`GameDownloadModal`、`GameSettingsModal` 的部分控制流继续下沉到 composable / pure helper，降低页面层“控制器化”程度。
- `GameSettingsModal` 的加载 session、自动保存、切换游戏回写和 info 未保存确认进一步收敛，减少重复写入和切换时序竞争。
- 首页启动守卫继续拆为“纯策略计划 + UI 执行器”两层，减少领域规则与弹窗/导航耦合。
- 清理一批前端 `any`、`EventListener` 强转与模板样式 `as any`，提升 `vue-tsc` 类型覆盖收益并减少展示层噪音代码。

2026-03-08 0.0.8

新增
- 新增完整的 Windows 原生 `ssmt4-bridge.exe` 工程与构建链路，包含桥接配置解析、路径转换、`d3dx.ini` 配置、DLL 注入、游戏初始化、包部署、进程跟踪、状态上报、签名校验和本地存储等模块。
- 新增 `3DMigoto` 全局总开关，默认关闭；关闭后会全局禁用相关配置、Bridge 和注入链，同时隐藏游戏设置里的 3DMigoto 入口。
- 新增顶栏 `Mod 管理` 页面，仅在启用 3DMigoto 全局功能后显示，并支持路由访问控制。
- 新增按游戏管理 Mod 的完整工作流：游戏切换、搜索、状态统计、单项启用/禁用、批量启用/禁用、打开 `Mods` / `ShaderFixes` 目录、切换单游戏 3DMigoto 开关。
- 新增后端 Mod 管理命令与目录状态扫描，统一返回 importer、有效目录、条目状态、大小、修改时间和启用情况。
- 新增内置文档体系与本地打包 Markdown 文档资源，覆盖 `Home`、服务条款、项目风险、游戏下载、下载/校验/修复机制、Prefix、Proton、DXVK / VKD3D、防护、已知问题、日志排查、Mod / 3DMigoto / Bridge 高级工作流等文档。
- 新增 3DMigoto 风险确认、严重警告、实验性标识与《服务条款》入口，并支持从设置页直接跳转查看。
- 新增 `VKD3D` 作为独立但与 `DXVK` 并列的图形组件管理对象和文档条目。

3DMigoto、Bridge 与 Mod 管理
- 建立“全局开关 -> 单游戏开关 -> Bridge 启动链 -> Mod 目录状态”的完整联动模型，避免出现页面显示开启但运行时实际跳过注入的状态错位。
- 3DMigoto 设置新增路径自动识别、自动推导与恢复自动值能力，可自动解析 `migoto_path`、`importer_folder`、`mod_folder`、`shader_fixes_folder` 和 `d3dx_ini_path` 的关系。
- 已知游戏的 importer 改为按预设强制校正，统一映射 `WWMI`、`ZZMI`、`SRMI`、`GIMI`、`HIMI`、`EFMI`，降低错误 importer 造成的无效注入。
- 修正 importer 默认行为差异，补齐 EFMI 的默认参数语义和旧配置纠偏逻辑，避免旧值沿用导致 `process_timeout`、`use_hook`、`enforce_rendering`、`xxmi_dll_init_delay` 等行为异常。
- 适配终末地官方启动链，支持通过 `Launcher.exe` 启动并以 `Endfield.exe` 作为真实注入目标，同时规范官方启动参数。
- Bridge 配置生成链路增强：自动写出 `bridge-config.json`，统一推导 Mods / ShaderFixes / d3dx.ini 路径，并在游戏退出后回读 `bridge-output.log`。
- 3DMigoto 启用时自动补充必要启动参数和 DX11 运行前提；同时在未显式覆盖时自动设置 `DXVK_ASYNC=1`，降低首次着色器编译卡顿。
- `Mod 管理` 页面实际采用目录重命名方式管理启用状态，通过 `.disabled` 后缀完成安全启用/禁用，而不是直接删除内容。
- 新增 importer 包管理与工具链支持，为后续更稳定的 XXMI/3DMigoto 部署奠定基础。

界面、交互与国际化
- 修复前后端通信链路中的交互问题，并缓解设置页的卡顿和多面板压力。
- `Mod 管理`、文档中心和 3DMigoto 相关界面补充多语言文案，游戏选择支持中文、繁体、英文等语言显示，而不是固定硬编码名称。
- 文档页改为统一文档清单驱动，支持本地 Markdown 索引、Home 跳转、文档目录统一注册以及随前端构建一起打包。
- `3DMIGOTO 管理` 增加严重警告区域、实验性标识、条款入口和风险重申交互，降低高风险功能误用概率。
- 文档页显示、换行和滚动问题完成修复，解决内置条款文档初版的渲染混乱与无法滚动到底部的问题。

文档与说明体系
- 重写并深化 `Home` 文档，补充新手路径、排障路径、页面职责和打包说明。
- 深化 `项目风险与要求`、`游戏下载与主程序配置`、`Proton 下载、管理与使用`、`DXVK / VKD3D 下载、管理与使用`、`防护与防封禁管理`、`已知问题与不足` 等核心文档。
- 新增 `日志分析与标准排查流程`、`Prefix 与模板管理`、`下载、校验、修复机制与厂商差异`、`Mod 管理与 3DMigoto / Bridge 高级工作流` 等进阶文档。
- 文档内容改为按当前项目真实实现编写，不再停留在简单操作说明层面，而是覆盖配置层、运行层、厂商差异、风险边界和标准排查流程。

启动链路、性能与稳定性
- 优化启动链路与首屏加载性能，改善部分页面和启动阶段的阻塞感。
- 修正生产构建默认值与发布产物整理逻辑，降低正式包误带开发配置、旧身份标识或冗余产物的概率。
- 强化 3DMigoto、Bridge 和启动链日志的配置一致性，便于后续排查路径、注入、官方启动链和 importer 偏差问题。
- 完善终末地、鸣潮等游戏的启动识别和兼容逻辑，降低误选启动器壳程序、错误 EXE 或不正确启动链导致的失败率。

工程与打包维护
- 国际化资源重组到新的 `internationalization/i18n/` 结构，统一语言包来源和加载逻辑。
- 更新 `pnpm` 依赖与构建流程，继续标准化前端构建和正式包产物输出。
- `version`、`version-log` 与文档体系联动完善，形成更完整的版本记录与发布说明基础。
